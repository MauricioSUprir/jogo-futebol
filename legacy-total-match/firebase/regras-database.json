{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",

    "admins": {
      ".read": "auth != null",
      ".write": false
    },

    "users": {
      "$uid": {
        "coins": {
          ".validate": "newData.isNumber() && newData.val() >= 0",
          ".write": "auth != null && ( root.child('admins').child(auth.uid).exists() || ( $uid === auth.uid && ( !data.exists() || newData.val() <= data.val() || ( newData.val() - data.val() <= 250 && newData.parent().child('coinGate').val() == now && ( !data.parent().child('coinGate').exists() || now - data.parent().child('coinGate').val() > 8000 ) ) ) ) )"
        },
        "coinGate": {
          ".validate": "newData.isNumber() && newData.val() == now",
          ".write": "auth != null && ( $uid === auth.uid || root.child('admins').child(auth.uid).exists() )"
        }
      }
    }
  }
}
