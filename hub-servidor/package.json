{
  "name": "hub-familia-servidor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Hub da Família — servidor com login, banco de dados e sincronização entre celulares.",
  "scripts": {
    "start": "node server.mjs",
    "seed": "node seed.mjs",
    "dev": "node --watch server.mjs"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "postgres": "^3.4.5"
  }
}
